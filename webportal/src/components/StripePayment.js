import React from "react";

const StripePayment = () => { // Add prop for success/failure of transaction
    return (
        <div>
            <h1>Stripe Payment</h1>
        </div>
    )  
}

export default StripePayment; 